#!/bin/sh

# configure git
echo "\nSetting up Git"
git config --global user.name "Dan"
git config --global user.email "vareladanmarj@gmail.com"
git config --global core.editor "nvim"
git config --global init.defaultBranch "master"
